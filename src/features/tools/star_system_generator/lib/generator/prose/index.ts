// Barrel for narrative-prose helpers. Re-exports are added per task in the
// Phase 0 plan as each source file is created (see NARRATIVE_GRAPH_PHASE_0_PLAN.md).
// Use named re-exports (e.g. `export { foo, bar } from './module'`), not `export * from`,
// to avoid silent symbol collisions across files.
export {}
